## Packages
framer-motion | For smooth animations and entry effects
canvas-confetti | For celebration effect when copying code
lucide-react | For beautiful icons (already in base stack but good to note)
date-fns | For time formatting if needed

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
